# PES6 自建服务器 · 使用说明

> **PChost** = 主机 A 机（跑服务器、有公网 IP）｜ **PCclient** = 客机 B 机
> 修改人：zhangdansan(stevoo) ｜ 最后更新：2026-09-05 ｜ 本文档随每次改动同步更新
>
> **标注约定（持续要求，本文档所有结论必须遵守）**：
> 【已验证】= 本次实测过；【高可能】= 公开资料/社区经验一致但未实测；【待验证】= 必须实际测试才能下结论。
> 未经实测的结论不得标【已验证】；【待验证】项在实测通过后须更新为【已验证】并注明日期。
>
> **目录结构（2026-09-05 分居）**：`D:\Games\friendPES6\dev\` = 开发区（游戏本体+pes6server+git 仓库，本文档路径均相对此处）；`D:\Games\friendPES6\release\` = 发布区（make_release.bat + friendPES6_release1.0 输出包，主机/客机双角色可用）
> **文案约定（持续要求）**：全部脚本与文档的方括号一律用全角【】（含 [OK]→【OK】、步骤号→【1/6】）；
> 括号块内的 echo 文本禁止半角圆括号，一律用【】（半角 ) 会被 cmd 当作块结束符）。

## 一、这套东西是什么

在 PChost（有公网 IP 的那台电脑）上跑一个 PES6 联机大厅服务器（Fiveserver 0.4.8 社区开源版，PES6 模式俗称 Sixserver）+ 自建 STUN 应答器 + 便携版 MySQL。

- 全部"双击 bat"启动/关闭，不依赖 PVE / Docker / 任何第三方服务
- PCclient（朋友那台）通过互联网连进来，账号自己注册

## 二、环境要求

### PChost 主机（A 机）

| 项目 | 要求 | 说明 |
| --- | --- | --- |
| 操作系统 | Windows 10 / 11 | 已实测 Win10 |
| Python 3.x | **需要**（STUN 应答器用） | 已装 3.13 ✓；重装系统后装一个即可，勾选 Add to PATH |
| curl | Win10 系统自带 | 取公网 IP 用（双源在线检测） |
| MySQL / 大厅服务 | 已内置便携版 | 随 `pes6server` 文件夹分发，零安装 |
| 网络 | **真实公网 IPv4**（PPPoE 桥接拨号） | 已实测无运营商级 NAT |
| 路由器 | ikuai 后台权限 | 配置 DMZ 或端口映射（见第六节） |
| 目录布局 | 游戏文件夹与 `pes6server` **平级** | 一键启动默认按 `..\PES6.exe` 找游戏（找不到会提示手输路径） |
| 防火墙 | 开启时需放行游戏端口 | 跑 `PC_host_4` 脚本或允许弹窗 |

### PCclient 客机（B 机）

| 项目 | 要求 | 说明 |
| --- | --- | --- |
| 操作系统 | Windows 10 / 11 | — |
| 网络 | **家庭宽带**（电信/联通/移动均可） | ⚠ **手机热点不可用**：端口受限 NAT 过不了 STUN 检测（已实测无解） |
| Python | **不需要** | 家宽方案客机零脚本依赖 |
| 服务器组件 | 不需要运行 | `pes6server` 文件夹拷过去只是为一键脚本，其中服务组件闲置 |
| 游戏本体 | 与主机**同一版本**的游戏文件夹 | 首次一键启动会**自动运行 `install.bat`**（已随 `pes6server\scripts\` 分发），无需手动 |
| settings.exe | UDP 端口固定 5730（取消"自动"勾选）、UPnP 不勾 | 端口值一键流程会自动补丁并先备份；"自动"勾选与 UPnP 需在 settings.exe 手动取消一次 |
| 防火墙 | 允许 PES6.exe | 首次联网弹窗"专用+公用"都勾，或临时关闭 |
| 自家路由器 | 一般无需配置 | 若客机也卡 573x（NAT 偏严格），在客机自家路由器给它开 DMZ/端口映射 |
| 账号 | 自己注册一个 | 不能与主机同时使用同一账号（会顶号） |

### 目录与文件结构（2026-09-05 分居后实测对齐）

```
D:\Games\friendPES6\
dev\                              ← 开发区(游戏+项目+git 仓库, 本文所有相对路径基于此)
  PES6.exe / settings.exe          ← 游戏主程序与设置
  dat\                             ← 游戏数据(1.4G)
  pes6server\                      ← 项目主体
    一键启动.bat / 一键关闭.bat     ← 双击入口(先选角色 主机/客机)
    README-部署说明.md              ← 本文档
    scripts\                        ← 分步脚本(一键流程内部调用, 也可单独跑)
      install.bat                  ← 注册表写入(一键启动自动运行; 新目录自动重注册)
      PC_host_1_启动服务器.bat / PC_host_1_关闭服务器.bat
      PC_host_2_注册账号-打开网页.bat
      PC_host_3_改hosts指向本机.bat / _还原.bat
      PC_host_4_添加防火墙放行.bat / _还原.bat   ← 方案1 后休眠(防火墙规则常驻)
      PC_host_5_设置ikuai端口转发.bat / _还原.bat
      PC_client_1_改hosts.bat / _还原.bat        ← 客机用
    mysql\                          ← 便携 MySQL 5.7.44(1.4G, data 内含账号库)
    fiveserver\                      ← 大厅服务 Fiveserver 0.4.8
      etc\conf\sixserver.yaml     ← 服务配置(ServerIP/端口)
      etc\keys                     ← 协议密钥
      log\sixserver.log            ← 大厅日志
    stun\                           ← 自建 STUN 应答器
      stun_server.py                 ← 主程序
      log\stun_run.log             ← STUN 日志
    _archive\                       ← 归档(早期文档/构建物, 不入库)
\release\                           ← 发布区(仓库外)
  make_release.bat                  ← 双击: 同步 dev 到下方输出包
  friendPES6_release1.0\            ← 输出包(2.8G, 主机/客机双角色可用)
```
## 三、快速开始

### PChost 主机

1. 双击 `一键启动.bat` → 输入 `1`（主机）
2. 流程自动：启动服务（已运行跳过）→ 检查 hosts（没指对本机就自动改）→ 检查防火墙放行（没加就自动加）→ 询问路由器是否已设置 → 检查账号数（不足 2 个会打开注册页）→ 最后询问是否启动游戏
3. 进游戏 NETWORK → 网络测试 → 登录 → 进大厅

### PCclient 客机（家庭宽带环境）

1. 拷贝游戏文件夹 + `pes6server` 文件夹到客机
2. 首次运行一键启动时会**自动执行** `install.bat` 写注册表（新电脑无需手动）
3. 双击 `pes6server\一键启动.bat` → 输入 `2`（客机）
4. 流程自动：检测 install.bat → 检查/补丁 settings.dat（UDP 5730 自动补丁；"自动"勾选与 UPnP 需在 settings.exe 手动取消）→ 改 hosts 指向公网 IP → 防火墙提示 → 浏览器验证链路 → 询问启动游戏
5. 进游戏 NETWORK → 网络测试 → 登录 → 进大厅 → 加房对战

### 关服 / 断开

- **主机**：双击 `一键关闭.bat` → 输入 `1`（自动停服 + 还原 hosts + 删防火墙规则，最后打印路由器还原指引，按提示手动撤销 DMZ/映射）
- **客机**：双击 `一键关闭.bat` → 输入 `2`（自动还原 hosts）

## 四、分步脚本说明

`scripts\` 里的脚本是一键流程的"零件"，正常情况不需要手动跑；单独双击也完全可用（行为与一键流程一致）。

| 正向 | 还原 | 版本 |
| --- | --- | --- |
| PC_host_1_启动服务器 | PC_host_1_关闭服务器 | v1.0 |
| PC_host_2_注册账号-打开网页 | — | v1.0 |
| PC_host_3_改hosts指向本机 | PC_host_3_改hosts指向本机_还原 | v1.0 |
| PC_host_4_添加防火墙放行 | PC_host_4_添加防火墙放行_还原 | v1.0 |
| PC_host_5_设置ikuai端口转发 | PC_host_5_设置ikuai端口转发_还原 | v1.0 |
| PC_client_1_改hosts | PC_client_1_改hosts_还原 | v1.0 |

一键启动 / 一键关闭 当前均为 v1.0 (2026-09-05)。每个 bat 运行时会在窗口首行打印自己的版本号；版本变更规则：功能或行为有实质变化时递增小版本（v1.1），仅文案微调递增修订（v1.0.1）。

还原脚本都会：先检测现状 → Y/N 确认 → 执行 → 再验证并打印结果。

## 五、账号注册（双方各自有账号；同一账号两台机器同时登录会顶号）

浏览器打开 `http://127.0.0.1:8190/`（客机上则用 `http://公网IP:8190/`）

- 用户名：3 位以上字母数字
- 序列号：20 位游戏安装码，如 `A6V9D5HXPT62H4PFWA45`
- 密码：3 位以上

**游戏内登录格式（重要）**：

- 序列号栏 = 注册时的 20 位序列号
- 密码栏 = `用户名-密码`（减号连接，例如 `zhangdansan-abc123`）

哈希公式（与游戏客户端完全一致，已实测）：`md5(序列号大写去横线 + 补NUL到36位 + 用户名 + '-' + 密码)`

## 六、ikuai 路由器（PChost 侧，一次性；PC_host_5 脚本会打印同样内容）

端口转发（指向 PChost 内网 IP，如 `192.168.50.113`）：

| 协议 | 端口 |
| --- | --- |
| TCP | 8190, 8191, 10881, 20200, 20201, 20202, 20203 |
| UDP | 3478, 3479, 5730-5740 |

- 测试期可用 DMZ 代替；正式玩建议撤 DMZ 只留以上精确转发
- 建议：给 PChost 内网 IP 做 DHCP 静态绑定，防止 IP 漂移

## 七、公网 IP 变化（约每周一次）

重跑 `一键启动.bat`（主机）会在线实测新 IP 并写进配置和 STUN（双源检测：3322 两个源，都取不到会明确报错停止，不使用缓存文件）；客机重新双击 `一键启动.bat`（客机），按提示输入新 IP 即可（IP 以主机一键启动窗口显示的为准）。

## 八、已知限制与说明

- 手机热点（蜂窝 CGNAT）作为客机网络时，STUN 检测过不去（端口受限型 NAT，服务端已尽力，实测无解）——**客机请用家庭宽带**
- 客机家宽的路由器若也是端口受限型（少见），在客机自家路由器上给客机电脑开 DMZ/端口映射即可解决
- STUN 的"换IP"语义在单公网 IP 下用"换端口"替代（已实测可通过）
- 游戏内登录报 verification 错误：看 `fiveserver\log\sixserver.log` 的 authenticate_3003 段落定位
- MySQL 曾被强制断电/杀进程：InnoDB 会自动恢复，实测数据无损

## 九、安全收紧清单（联机跑通后建议执行）

1. ikuai 撤 DMZ，只留第七节的精确转发
2. Windows 防火墙全开 + 跑 `PC_host_4_添加防火墙放行.bat`
3. 修改管理页密码：`fiveserver\etc\conf\admin6.yaml`（默认 fives/fives）
4. 账号注册齐后，删除 ikuai 上 8190 端口的转发（关闭公网注册通道）
5. 定期备份 `mysql\data` 文件夹（账号与战绩都在里面）

## 十、排障速查

- **对战开始黑屏 = P2P UDP 握手失败**：登录/进房走服务器 TCP，对战是双方 P2P UDP(5730)。大厅日志中玩家的 udpPort1(公网出口端口)若 ≠ 5730(如被 NAT 改写为 30660)，主机按通告端口回发的对战流会被对方路由器丢弃【已验证 2026-09-05 实战：Andriy_Han udpPort1=30660 → 黑屏】。解决=在**对方**路由器给他开 DMZ 或 UDP 5730 映射，改后 udpPort1 变 5730 即生效
- **客机 ping 公网 IP 超时 ≠ 链路不通**：ping 走 ICMP，ikuai/Windows 默认丢弃 ICMP 而 TCP 正常放行【已验证 2026-09-05：外部节点 ping 全超时但 TCP 8190/10881 秒连】。正确验证方式=浏览器开 `http://公网IP:8190/` 看注册页，或直接游戏内网络测试

| 现象 | 看哪里 |
| --- | --- |
| 连接测试报"无法连接服务器"（STUN 阶段失败） | 客机 hosts 是否指向公网 IP；ikuai 转发/DMZ；PChost 防火墙是否放行 |
| 报 "Unable to transmit using UDP port 573x"（573x 收发检查失败） | 主机侧 DMZ/端口映射（UDP 5730-5740）是否生效 |
| 登录报 verification 错误 | `fiveserver\log\sixserver.log` 的 authenticate_3003 段落 |
| 大厅进不去但测试通过 | TCP 20200/20201/20202 转发 |
| 一键启动里出现 [X] 项 | 按提示手动跑对应分步脚本 |

## 十一、待办事项（TODO）

### 待验证 · 明天

- [ ] 一键启动.bat 主机模式全流程实测（含 MySQL 修复后的真实启动）
- [ ] 一键启动.bat 客机模式全流程实测（B 机家庭宽带）
- [ ] 一键关闭.bat 主机/客机两种模式实测（含还原效果确认）
- [ ] 游戏 A↔B 实际对战一局，确认 P2P 直连与延迟
- [ ] 对战确认后执行第十节安全收紧，并复测一切正常

### 后期增强路线

**⑧ 中文汉化**

- 【进行中 2026-09-06】用户已将中文版 e_text.afs(24.8MB, MD5=1fbc8603f54cfe7099c6df874b212d8a) 替换进 dev 与 release 两侧 dat`，双侧 MD5 一致 → roster hash 两端同步变化、互不冲突；afs2fs(afs.root="dat") 无松散覆盖目录，替换生效
- 【待办】新 e_text.afs 必须发给朋友覆盖其 dat 内同名文件(英文旧版)，否则 roster hash 分叉 → 开局同步失败
- 原英文版未入 git(dat/ 排除)；D:\Games 下其他 PES6 目录有同版英文文件可还原
- "联机必须英文文本"的旧说法：双侧同版即可，中英文本身不设限【待验证，以明日实战为准】

**⑨ Kitserver**

- PES6 通用 mod 框架（球衣/球场/足球/参数/lod）
- 版本已确定【已验证 2026-09-05 官方 history.txt】：权威版本源=kitserver.mapote.com/ks6/history.txt，最新为 **6.8.2**(2021-04-22, skinserver+窗口化)，其后为 6.8.1(2020-05-05)/6.8.0(2020-05-02, juce&robbie, 用户手中持有, 联机功能完全够用)；6.8.x 二进制经社区渠道分发(EvoWeb/Facebook EFLPatchPES6 的 mediafire)，mapote 官方文件库仅托管到 kitserver-572；源码：github.com/kitserver/kitserver6
- 安装：解压至游戏根目录(dev)→运行 setup.exe；两台机器必须同版本；装完后 make_release 复制清单需补 kitserver 目录及根目录新增 dll【待办】
- 官方 howto 提到 kitserver 的 network.cfg 可直接指定 network.server=服务器地址——在 PES6 下是否同样生效【待验证】，若可用可替代改 hosts 的方案

**⑩ 统一 Option File**

- 双方存档/编辑数据必须完全一致，否则对战时 roster hash 对比失败（sixserver 配置 Roster.compareHash=true）
- 方案：在 PChost 上定稿 OF 后，随打包统一分发

**⑪ 最终打包给朋友**

- 打包与同步：双击 `D:\Games\friendPES6\release\make_release.bat`，把 `dev` 同步到 `release\friendPES6_release1.0\`；内容=游戏本体(PES6.exe/settings.exe/dat)+pes6server 完整组件(主机+客机双角色：一键启动/一键关闭/scripts/mysql/fiveserver/stun/README)；自动排除 _archive、日志、编译缓存；robocopy /MIR 增量+关键文件校验(主机与客机各一组)
- 附精简说明：注册地址/游戏内登录格式/常见问题三条
- 验收标准：找一台全新电脑，按包内说明从零走到进大厅

### 后续可选

- [ ] 热点场景后手：客机本地 UDP 中继脚本（未实施，需要时再做）
- [ ] DDNS 自动化：公网 IP 变化时客机 hosts 自动跟随（现为手动）
- [ ] 日志改名：`fiveserver\log\sixserver.log` → 更直观的名字（待确认）
- [ ] 管理页默认密码修改（admin6.yaml，fives/fives → 自定义）
- [ ] mysql\data 定期备份（账号/档案/战绩）
- [ ] 二进制等长补丁："SYSTEM: Fiveserver v0.4.8" 横幅字样定制（仅外观）
- [ ] UPnP 标志位定位：拿到"勾选/不勾"两份 settings.dat 做 diff 找到标志字节后，客机流程可自动关闭 UPnP（现为一键提示+手动）

### 明日联机清单（2026-09-06，按此执行不遗漏）

**主机侧（用户动手）**
1. 装 Kitserver 6.8.0：解压到 `dev\`（PES6.exe 同目录）→ 运行 setup.exe → 游戏启动见 kitserver 横幅
   （直链 MEGA：https://mega.nz/file/JV01zAab#g2vjKk0yqdNsQGd8VikBySgj3ucuoMaO98ckAl2_qrU ）
2. 装完告诉 ZCode：补 make_release 复制清单（kitserver 目录 + 根目录新增 dll）→ 同步 release
3. 把 `kitserver\` 文件夹 + 根目录新增 dll 通过 QQ/网盘发给朋友，朋友放进他的游戏根目录（两边版本必须一致）
4. 主机栈重启：release\...\一键关闭.bat → 一键启动.bat，主窗口确认【OK】 STUN 中继 5731 已监听

**ZCode（开赛时）**
1. 确认 5731 监听 + 中继日志出现两个端点（主机回环端点 + 朋友公网端点）
2. 赛后拉取：中继统计(双方收包数)/STUN 探测/sixserver 房间事件/防火墙收发记录
3. 判定黑屏是否终结 → 更新 README 结论 → 提交

**朋友侧（转告，共 3 件）**
1. 完全退出游戏重新启动、重新登录 Andriy_Han（必须，重新 STUN 拿中继地址）
2. 把收到的 kitserver 文件放进他的游戏根目录（与主机同版本）
3. 防火墙保持关闭/360 保持退出（或给 PES6 放行）；他家光猫**无需任何配置**（中继已绕开）
4. 用 QQ 接收主机发来的**新 e_text.afs(24.8MB 中文版)**，放进他游戏根目录 `dat\` 覆盖同名文件（版本不一致会导致开局 roster 比对失败）

**判定标准**
- 开赛后 stun_run.log 中继统计出现双方端点且收包数持续增长 = 对战流经中继互通
- 若中继统计双向正常但仍黑屏 → 问题不在网络，转查游戏本体（Win7 兼容/游戏报错截图）

## 十二、Git 版本管理

- 仓库根：`D:\Games\friendPES6\dev`（游戏 + pes6server 整体管理；release 在仓库外），分支 `main`
- 提交身份（仓库级）：`zhangdansan(stevoo)` / `stevoo@users.noreply.github.com`
  —— 推送 GitHub 前建议把 email 换成真实邮箱：`git config user.email "你的邮箱"`
- **不入库内容**（.gitignore 已配置）及原因：

  | 排除项 | 原因 |
  | --- | --- |
  | PES6.exe / settings.exe / dat\ / opmov\ | Konami 版权文件，公开仓库有 DMCA 风险；AFS 单文件数百 MB 超 GitHub 限制 |
  | pes6server\mysql\（程序+数据） | MySQL 程序 1.3GB 远超限制；data 含账号数据 |
  | 各日志目录、_archive\_build\ | 体积大且非交付物 |

- 因此 GitHub 仓库只含"脚本 + 配置 + 文档 + 小体积服务端件"；**全新机器部署时，MySQL 程序与游戏本体需另行分发**（与⑪最终打包一致）
- 推送 GitHub 步骤（届时）：GitHub 建仓库（建议 **Private**）→ `git remote add origin https://github.com/<用户名>/<仓库名>.git` → `git push -u origin main`

## 十三、修改记录（修改人：zhangdansan(stevoo)）

| 时间 | 内容 |
| --- | --- |
| 2026-09-04 22:05 | 建立联机方案文档，确认拓扑与"自建大厅服务器"路线 |
| 2026-09-04 23:03 | 获取 Fiveserver 0.4.8 官方 Windows 包与 evo-league 源码 |
| 2026-09-04 23:16 | 首次跑通 MySQL 5.7 + Fiveserver（全部端口就绪） |
| 2026-09-04 23:17 | 注册页按客户端哈希公式定制完成，测试账号注册入库成功 |
| 2026-09-04 23:27 | STUN v3 修复备用应答通告一致性 → A 机通过网络测试进大厅 |
| 2026-09-04 23:31 | 补齐数据库 comment 列与 settings 表 → 游戏登录成功 |
| 2026-09-05 00:07 | 新增公网来源兜底应答；判定 B 热点为端口受限 NAT（无解），确认 PCclient 需使用家庭宽带 |
| 2026-09-05 00:36 | 停止全部服务，A 机还原干净状态 |
| 2026-09-05 00:45 | 目录整理：过程文件归档至 _archive，stun 独立成文件夹 |
| 2026-09-05 00:52 | 脚本命名规范化（PC_host/PC_client 前缀） |
| 2026-09-05 01:10 | 全部状态类操作配齐还原脚本（hosts/防火墙/ikuai/客机） |
| 2026-09-05 01:20 | 重构为"一键启动/一键关闭 + scripts 分步脚本"结构：角色选择、智能跳过、步骤编号、ikuai 按键等待 |
| 2026-09-05 01:35 | 修复实测暴露的 4 个 bug：无 cd 行脚本解析器错位重建、括号提前闭合改【】、围栏行补 echo 前缀、MySQL 启动改轮询等待+延迟展开修"此时不应有 10" |
| 2026-09-05 01:45 | 修复 my.ini 路径被写坏问题：改用 cd 后真实绝对路径（对启动上下文免疫）+ 组件定位校验保险丝；goto :EOF 修正 + cmd /k 防闪退保护壳；经 bash 通道拉起全部服务并验证（数据完好，2 账号） |
| 2026-09-05 01:55 | 待办补充后期路线 ⑧中文汉化 ⑨Kitserver ⑩统一Option File ⑪最终打包给朋友（含各自要点与注意坑） |
| 2026-09-05 02:05 | STUN 日志双通道（控制台+文件）并归位至 stun\log\stun_run.log；废除 当前公网IP.txt 机制（公网 IP 改双源在线实测，失败即停）；客机流程改为手输 PChost 公网 IP；修复 PC_host_5 指引文字中 "->" 被 cmd 当作重定向、产生垃圾文件的问题（箭头改为 ──）；run_test.txt 归档至 _archive\logs\ |
| 2026-09-05 02:15 | README 转为 Markdown 格式；文件结构图与实际目录逐行对齐（补 etc\keys、mysql 程序本体、_archive 子项） |
| 2026-09-05 02:25 | 新增第二节【环境要求】：主机/客机分开列表（Python、curl、网络类型、install.bat、settings.exe、防火墙、路由器 NAT 等），后续章节顺延重编号 |
| 2026-09-05 02:40 | 一键启动客机流程补全 settings.exe 说明（UDP 端口取消"自动"勾选填 5730、UPnP 不勾、补丁后提示手动确认）；全部 10 处输入提示加"(输入后按回车)"；修改人署名更新为 zhangdansan(stevoo) |
| 2026-09-05 02:55 | 提权链修复：6 个提权脚本 SELF 路径双保险解析 + 提权目标改 cmd /k 常驻窗（UAC 后新窗口不再闪退）+ 提权后 pushd 回脚本目录；全库 13 个 bat 行尾规范化（清除 CRCRLF 畸形行尾） |
| 2026-09-05 03:10 | "被吃字符"清剿：清除 0x08/0x0C 控制字节共 4 处（mysql.exe 路径、sixserver.yaml 写入行等）；[1/6] 调用链改 PES6_ROOT 环境变量交接（对 %~dp0 失效免疫）；游戏启动改 start /D 游戏目录（修复"afs file not found"）；my.ini 生成器补 log_syslog=0；[4/6] 输错重问 |
| 2026-09-05 03:25 | 界面简化：步骤围栏从上下双行改为仅上方单行（一键启动/一键关闭共 20 处）；修复简化过程中误删步骤标题行的问题，全部标题与流程结束行已验证就位 |
| 2026-09-05 03:40 | 输错重问全覆盖：角色选择（1/2）、启动游戏（Y/N）、自定义游戏路径均改为循环重问（不再输错退出）；自定义路径启动同样带工作目录保护（防 afs 报错） |

| 2026-09-05 03:50 | 全部 13 个 bat 加版本号 v1.0 (2026-09-05)，运行时首行打印；根目录两个入口的版本行置于 /inner 包装层之后（避免重复打印） |
| 2026-09-05 04:00 | 一致性审计：13 个 bat 全部落实历轮修改意见（版本/重问/回车提示/CRLF/控制字节/PES6_ROOT/提权常驻窗等 30+ 项全过）；项目纳入 git 管理（分支 main，首提交 54 文件，.git 5.8MB），排除游戏本体/MySQL/日志/归档大件；新增第十二节 Git 版本管理 |


| 2026-09-05 04:20 | 隐私清理：删除遗留的 4 个过程脚本；归档方案文档改名并清洗标题；全库无任何工具来源痕迹；目录改名 zhipuPES → friendPES6（README/my.ini 等引用同步），仓库根更新为 D:\Games\friendPES6 |
| 2026-09-05 04:30 | 更正错误结论："无需注册表即可运行"说法作废——新机器首次必须运行 install.bat 写入 `KONAMIPES6` 注册表键，否则游戏打不开（B 机实测）；README 正文各处此前已按正确流程描述，本次仅修正早期版归档文档 |
| 2026-09-05 11:10 | 文档头部新增【标注约定】为持续要求：【已验证】= 实测过；【高可能】= 资料/社区经验一致未实测；【待验证】= 必须实测后才能下结论；未实测不得标已验证，实测通过后须升级标注并注明日期 |

| 2026-09-05 11:35 | 修复 [6/6] "现在启动游戏吗" Y/N 死循环：原逻辑 Y 分支执行后落入 `if /i not "%G%"=="N"`，回答 Y 永远被判非法重问（主机/客机两段同样问题）；改为 Y/N/非法三路 goto 跳转。顺带修复手输游戏路径分支 %GP% 在括号块内被解析期展开为空的 bug（移出括号块，实测路径可正确解析盘符目录） |

| 2026-09-05 11:41 | 修复步骤间双分隔线：每个步骤结尾多输出一组 echo.+分隔线，与下一步骤开头的分隔线重复（主机段 4 处、客机段 4 处、一键关闭.bat 3 处），删除步骤结尾那组，步骤间恢复为一条分隔线 |

| 2026-09-05 11:55 | 修复【5/6】取账号数 bug：`for /f` 单引号命令中带引号路径+`COUNT(*)` 会遭 cmd /c 首尾引号剥离与括号截断，MySQL 正在运行却报"未运行"；改为结果写临时文件 + `set /p` 读取（实测两场景通过）。另：标题"PES6 一键启动/关闭"在分隔线内居中；客机选项行补 1 空格与主机行对齐；修复"全部完成!""完成!"的感叹号被 EnableDelayedExpansion 吞掉的问题（改 ^^!）

| 2026-09-05 12:07 | 修复主机游戏报"无法使用 UDP 端口 5730"：STUN v5 的 UDP 5730-5740 echo 应答器在 0.0.0.0 上抢绑 5730，一键启动先起 STUN 后开游戏，主机游戏永远绑不上端口（实测 netstat 证实 python 占 5730）；且对战时客机发往主机 5730 的 P2P 包会被 echo 吞掉。v6 整段移除 echo（客机不跑 STUN，该应答器无使用场景）。已重启 STUN 并回归测试通过（3478 探测应答正常，5730 空闲）。【待验证】游戏重启后进 network 大厅

| 2026-09-05 12:11 | 修复一键关闭主机流程两个 bug：① PC_host_1_关闭服务器.bat 组件定位检查误写成 `%ROOT%\%ROOT%`（pes6server\pes6server 双重拼接，检查永远失败报"未能定位组件"）；② 停 STUN 的 netstat UDP 按 tokens=5 取 PID，但 UDP 表 PID 在第 4 列，STUN 从来杀不掉（TCP 才是第 5 列）。均实测修复：起 STUN→跑脚本→"全部服务已停止"，无残留端口

| 2026-09-05 12:13 | 一键启动流程全部走完后窗口不再停留：因 cmd /k 防闪退壳会驻留命令行，现于主机/客机结尾及开头定位失败分支改为"按任意键关闭本窗口..."提示后 exit 关窗

| 2026-09-05 12:23 | ① 修复 UDP 5730 报错真正的根因：STUN 备用 socket 线程 lambda 多传一个参数（v5 遗留），直接探测 3479 的应答全部抛 TypeError，游戏网络测试第二阶段(CHANGE-REQUEST/改端口直探)收不到应答即报"无法使用 UDP 端口 5730"——此前只回归了 3478 主路径没测到该路径，教训：STUN 三条路径(3478 普通/3478 改变请求/3479 直探)必须全部实测。② 全库 13 个脚本 125 处 3 空格缩进改左对齐（层级缩进保留，标题居中与角色选项对齐保留）

| 2026-09-05 12:32 | 一键关闭提权重构：① 提权挪到流程最开头一次性完成(UAC 确认后整个 4 步在同一窗口执行)，修复【2/4】中途弹提权新窗口的问题；② 修复"系统找不到指定的路径"：6 个 hosts/防火墙脚本提权分支 pushd 后未 popd 就 exit，经 call 调用时把父脚本工作目录留在 scripts\ 下，后续相对路径 call 全部失败(启动流程同类隐患一并修)；③ 一键启动/一键关闭全部 call 改 %~dp0 绝对路径。模拟实测 popd 修复有效。【待验证】UAC 实际流程待用户下次运行确认

| 2026-09-05 12:40 | 一键启动同样改为开头一次性提权：此前 [2/6] 修改 hosts、[3/6] 加防火墙规则时会弹出提权子窗口(一键关闭还原过 hosts 后，下次启动必触发)，用户明确不要弹窗；提权后各脚本 net session 检查通过，全部步骤在同一窗口执行，两个提示文字同步更新。游戏/服务将由提权窗口拉起(以管理员运行)

| 2026-09-05 12:45 | 修复一键关闭刚加的提权提示导致整脚本报"此时不应有 ..."：echo 文本里的半角括号(...)在 if 括号块内会被 cmd 当作块结束符提前闭块，剩余 ... 成多余 token——这正是修改记录既有约定"括号提前闭合改【】"防的坑，本次新增文案违反了它；一键启动同款 3 处一并改为【】(含两处 [需要设置] 提示)。同构 mock 实测块内解析通过。教训重申：括号块内的 echo 文本一律用【】，禁止半角括号

| 2026-09-05 12:45 | 一键启动主机流程结尾新增提醒: 勿关任务栏最小化的 PES6-MySQL/PES6-STUN/PES6-Sixserver 服务窗口, 下线用一键关闭.bat

| 2026-09-05 12:49 | PC_host_5 两个脚本(设置/还原 ikuai 指引)删除脚本内部自带的上下双分隔线标题栏, 只留一行说明文字——分步脚本不再自带分隔线, 分隔线只由一键流程的步骤头输出, 步骤间保持一条

| 2026-09-05 12:50 | 一键关闭两处完善：①【4/4】"手动撤销完成后按任意键结束"改为 Y/N 询问是否已撤销路由器 DMZ/端口映射(选 N 提示路由器仍对公网开放请尽快撤销)，采用三路 goto 防重蹈 Y/N 死循环；② 修复主机/客机分支结尾不关窗：原 pause+goto :EOF 跑完停在 cmd /k 壳的命令行，改为"按任意键关闭本窗口"+exit

| 2026-09-05 12:56 | 修复【2/6】改 hosts 报 PowerShell ParserError(“if”语句缺少右括号)：4 个 hosts 脚本(PC_host_3/PC_client_1 各自的修改与还原版)的 PS 单行命令混用 \" 与 | 管道，被 cmd 二次解析截断——此前这些分支从未真正执行过，今日首跑即暴露。全部改为纯 cmd 实现(findstr /v 过滤 + 重定向追加 + copy 回写，追加前补保底空行)，用伪造 hosts 样本实测通过。教训：hosts 修改禁用 PowerShell 单行命令，一律 findstr/重定向

| 2026-09-05 13:00 | 全库 echo 文本前导空格清零(上次只清了恰好 3 空格，本次清理 1/2/5 空格等残留共 42 处，主要在 PC_host_5 两个指引脚本)；行内的表格列对齐保留

| 2026-09-05 13:02 | 全库统一方括号风格【持续约定】：括号内容为纯英文/数字用半角 [] (如 [OK]、[1/6]、[X])，内容含中文用全角【】(如【信息】、【跳过】、【主机】)；共修正 92 处，客机选项行随之重新对齐

| 2026-09-05 13:05 | 应用户要求推翻 13:02 的中英文分用规则，方括号全部统一为全角【】：[OK]→【OK】、[X]→【X】、步骤号→【1/6】等共 74 处；用精确 token 替换避免误伤 powershell 代码中的 $b[416]/[IO.File]；文案约定同步写入文档头部

| 2026-09-05 13:14 | 删除一键启动【2/6】【3/6】修改后多余的"完成后按任意键继续检测"暂停：该等待是提权子窗口时代的遗留(需等用户确认子窗口完成)，提权改为开头一次后全流程同窗口顺序执行，无需暂停

| 2026-09-05 13:21 | 输出对齐彻底清零第三批：块内 echo 源码缩进 110 处(之前保留导致【OK】等输出前带 2 空格)与行中 echo(如 && echo)后的多空格 11 处，全部压平；终检无残留。至此所有脚本的可见输出一律顶格

| 2026-09-05 14:10 | 【已验证 2026-09-05】修复登录报 Temporarily unable to connect：根因为 Windows 防火墙存在按程序命名的【阻止】规则(twistd/pes6.exe/python.exe/stun-server/GoalServer6 Alpha，源于历史弹窗点了取消)，阻止规则优先级高于端口放行规则 PES6-SERVER-TCP，服务器三大件入站全被拦，外网与本机回环均超时。经 netsh wfp show filters 导出过滤器清单定位，删除上述阻止规则后外网(法国/伊朗/土耳其/美国节点)与本机回环全部连通，防火墙保持开启。教训：①防火墙弹窗询问 PES6/twistd/python 必须点允许；②按端口放行对被阻止的程序无效，排障先查同名阻止规则；③定位命令链：丢包日志(pfirewall.log) → netsh wfp show filters 导出解析。另：排障临时新建的 PES6-TCP-in/PES6-UDP-in 与原名规则并存(无害)；丢包日志保持开启，日志文件 C:\Users\Nasa\pfirewall.log 与导出物 wfp_filters.xml 可随时删除

| 2026-09-05 14:18 | 防火墙自愈机制固化进脚本【已验证思路】：PC_host_4 每次运行先按名删除历史遗留的程序级阻止规则(twistd/pes6.exe/python.exe/stun-server/GoalServer6 Alpha)，再为 twistd/python/PES6.exe 建程序级允许规则(此后不再弹询问窗，防再次点叉埋雷)，端口规则重建逻辑不变；一键启动【3/6】由"规则存在即跳过"改为每次调用(脚本幂等)；一键关闭还原同步补删 PES6-app-* 规则。不采用整库 reset 方案(会误删用户其他规则)

| 2026-09-05 14:21 | 一键启动选择角色区新增防火墙弹窗操作提示：专用+公用都勾选再点允许，误点取消下次启动自动修复

| 2026-09-05 14:26 | 目录清洁：删除排障/测试残留(ask_test/endtest/hosts_test2.bat、sample_hosts、nul 文件、stun/__pycache__、_archive/_fix_yaml.py 及用户目录下 wfp_filters.xml)；.gitignore 补 __pycache__/ 防止编译缓存再入库

| 2026-09-05 14:39 | 一键启动主机/客机结尾由"按任意键关闭"改为 60 秒倒计时自动关窗(timeout /t 60)：期间按任意键中断倒计时并保留窗口(落在 cmd /k 提示符可回看输出)，倒计时自然结束则自动关闭

| 2026-09-05 14:56 | 实施方案二(消除每次启动弹防火墙询问窗)：①一键关闭还原只删端口放行规则，程序级放行规则 PES6-app-* 常驻保留；②一键启动把防火墙规则预置从【3/6】提前到【1/6】开头(先建规则再启服务，监听发生时规则已在，不再触发弹窗)，【1/6】标题同步更新；③【3/6】改为纯验证(端口+程序规则在位检查)。安全说明：服务关闭期间端口放行规则也随还原删除，无监听即无暴露

| 2026-09-05 15:00 | 【方案1 定稿】防火墙规则常驻：一键启动【1/6】的 PC_host_4 调用注释(规则不再增删；【3/6】保留只读验证)，一键关闭【3/4】注释并重编号为 3 步——此后关闭/启动循环不再触发防火墙弹窗(程序级放行规则常驻所致)。安全代价评估：服务关闭期间端口规则仍在但无进程监听，外部连接仅被拒绝，实际风险接近零；DMZ 大开仍是主要暴露面，正式使用建议 ikuai 改精确端口映射。恢复自动管理：取消两处 rem 注释即可

| 2026-09-05 15:17 | 修复客机【2/5】settings.dat 补丁报 PowerShell 缺右括号：脚本开启 EnableDelayedExpansion，PS 命令中 if(!(Test-Path... 的 ! 被延迟展开吞掉导致命令腰斩——客户端分支首次真正执行暴露。改用 -not 替代 !，全库扫描无其他同类隐患；用临时 settings.dat 实测补丁/备份/回读全部正确。教训：延迟展开环境下 PS/echo 文本禁用 ! 与 ^(，条件用 -not

| 2026-09-05 15:22 | install.bat 移入 pes6server\scripts\（git mv 保留历史）：GAME_DIR 由 %~dp0 改为解析上两级(实测路径归一化正确)，新增 /nopause 参数(自动调用不暂停, 手动双击仍带确认)；一键启动主机【1/6】与客机【1/5】均改为注册信息缺失时自动运行 install.bat(一键启动已提权, HKLM 写入无需二次 UAC)，客机不再要求手动双击；README 相关三处同步

| 2026-09-05 15:28 | 客机【5/5】链路验证改为 ping 公网 IP 6 次(仅显示结果不判断)，去掉浏览器打开与按键停顿，流程直落结尾 60 秒倒计时；顺带修复 PC_client_1_改hosts.bat 参数解析缺陷：位置参数(IP)在前时 /nopause 永远解析不到导致多停顿一次——parse_args 增加未知参数跳过

| 2026-09-05 15:34 | 修复客机【3/5】重复询问公网 IP：上轮参数解析修复把位置参数(IP)当作未知参数消费，脚本主体 set HOSTIP=%~1 时已为空导致再问一次。重构 parse_args：/nopause、/yes、位置参数三者在循环内分别捕获(IP 存入 HOSTIP)，主体删除 %~1 覆盖、仅缺失时交互询问；实测 IP 在前与 /yes 在前两种调用顺序均正确



| 2026-09-05 15:55 | 【目录分居】D:\Games\friendPES6 整树移入 dev\(含 .git，仓库根=dev)，release 移出仓库；make_release.bat v1.1 移至 release\ 并升级为双角色完整包(新增 mysql/fiveserver/stun，2.8G，排除 _archive/日志/缓存)；一键启动主机/客机注册检测升级为 installdir 与实际 GAME_DIR 比对(实测生效：本机注册表残留 zhipuPES 旧路径，下次启动自动重写为 dev 路径)；清理 15:45 条目转义损坏与重复

| 2026-09-05 16:10 | 补回丢失的目录结构图：02:15 记录声称已建结构图，但昨夜最后一次提交(04:30)中即已缺失(成因待考)；按分居后实测目录重建，插入第二节末尾，含 dev/release 全树与各文件用途

| 2026-09-05 16:27 | 【已验证 2026-09-05】修复发布包 MySQL 无法启动：MySQL 选项解析器会把 my.ini 值中的 \r 当转义(回车)，路径 \release\ 是全项目第一个撞上 \r 的目录(dev 的 \pes6server 等均安全)，datadir/basedir 被腰斩致 30 秒启动失败且不写 err 日志；PC_host_1 生成 my.ini 时路径统一转正斜杠(%ROOT_FW=%ROOT:\=/)，对任何目录名免疫；实测 release mysqld 以新 my.ini 正常监听 3306 并优雅关闭。另：release 内一键启动为旧版(注册检测无路径比对)，需重新同步；【3/6】校验改认 PES6-SERVER-* 与 PES6-TCP-in/UDP-in 两组规则名

| 2026-09-05 16:27 | 【已验证 2026-09-05】修复发布包 MySQL 无法启动：MySQL 选项解析器会把 my.ini 值中的 \r 当转义(回车)，路径 \release\ 是全项目第一个撞上 \r 的目录(dev 的 \pes6server 等均安全)，datadir/basedir 被腰斩致 30 秒启动失败且不写 err 日志；PC_host_1 生成 my.ini 时路径统一转正斜杠(%ROOT_FW=%ROOT:\=/)，对任何目录名免疫；实测 release mysqld 以新 my.ini 正常监听 3306 并优雅关闭。另：release 内一键启动为旧版(注册检测无路径比对)，需重新同步；【3/6】校验改认 PES6-SERVER-* 与 PES6-TCP-in/UDP-in 两组规则名

| 2026-09-05 19:33 | 【重要更正】客机【2/5】的"UPnP 需手动不勾"提示作废——那是主机(DMZ)语境的产物；实测实战黑屏定位：客机对战 P2P 依赖 UPnP 自动开 UDP 5730，取消 UPnP 恰好关闭小白玩家的默认联机机制。客机指引改为"UPnP 保持勾选(对战关键)"，黑屏排障路径=确认 UPnP→光猫超管开 UPnP/DMZ→Radmin VPN

| 2026-09-05 19:33 | 【重要更正】客机【2/5】的"UPnP 需手动不勾"提示作废——那是主机(DMZ)语境的产物；实测实战黑屏定位：客机对战 P2P 依赖 UPnP 自动开 UDP 5730，取消 UPnP 恰好关闭小白玩家的默认联机机制。客机指引改为"UPnP 保持勾选(对战关键)"，黑屏排障路径=确认 UPnP→光猫超管开 UPnP/DMZ→Radmin VPN

| 2026-09-05 20:05 | 【黑屏诊断挂起存档】朋友(Andriy_Han,Win7,陕联通家宽,光猫中兴G7615路由模式,无超管密码)已排除: 大厅TCP/STUN/房间P2P(3格)/roster一致/Win7防火墙已关/360已退。剩余唯一嫌疑: 光猫NAT端点相关过滤丢弃主机→朋友的对战流(九成把握,待抓包确证)。下次朋友有空的最短修复路径: ①打10010要超管密码(5分钟电话,零技术门槛)→DMZ给192.168.1.137; ②普通账号登录光猫后访问 /backupsettings.conf 下载配置搜CUAdmin(本机实测两个URL均被固件封禁,朋友侧可再试); ③Radmin VPN(已搁置的备选)。待办: 主机侧 allowedconnections 日志已请求开启,下次开赛读 pfirewall.log 确证 B→A 是否到达

| 2026-09-05 20:05 | 【黑屏诊断挂起存档】朋友(Andriy_Han,Win7,陕联通家宽,光猫中兴G7615路由模式,无超管密码)已排除: 大厅TCP/STUN/房间P2P(3格)/roster一致/Win7防火墙已关/360已退。剩余唯一嫌疑: 光猫NAT端点相关过滤丢弃主机→朋友的对战流(九成把握,待抓包确证)。下次朋友有空的最短修复路径: ①打10010要超管密码(5分钟电话,零技术门槛)→DMZ给192.168.1.137; ②普通账号登录光猫后访问 /backupsettings.conf 下载配置搜CUAdmin(本机实测两个URL均被固件封禁,朋友侧可再试); ③Radmin VPN(已搁置的备选)。待办: 主机侧 allowedconnections 日志已请求开启,下次开赛读 pfirewall.log 确证 B→A 是否到达

| 2026-09-05 20:05 | 【黑屏诊断挂起存档】朋友(Andriy_Han,Win7,陕联通家宽,光猫中兴G7615路由模式,无超管密码)已排除: 大厅TCP/STUN/房间P2P(3格)/roster一致/Win7防火墙已关/360已退。剩余唯一嫌疑: 光猫NAT端点相关过滤丢弃主机→朋友的对战流(九成把握,待抓包确证)。下次朋友有空的最短修复路径: ①打10010要超管密码(5分钟电话,零技术门槛)→DMZ给192.168.1.137; ②普通账号登录光猫后访问 /backupsettings.conf 下载配置搜CUAdmin(本机实测两个URL均被固件封禁,朋友侧可再试); ③Radmin VPN(已搁置的备选)。待办: 主机侧 allowedconnections 日志已请求开启,下次开赛读 pfirewall.log 确证 B→A 是否到达

| 2026-09-05 22:25 | PC_host_1 STUN 启动段增加主窗口确认：等 2 秒后实测 5731 是否监听，输出【OK】中继 5731 已监听/【X】未监听——中继状态不再只存在于最小化的 STUN 窗口

| 2026-09-05 22:51 | ⑨Kitserver 版本查证更正：此前"6.6.0 最终版/6.6.4 社区版"说法过时作废——用户已持有 6.8.0(juce&robbie 2020-04)，最新 6.8.1；网络查证下载源与安装要点已补入待办⑨

| 2026-09-05 22:56 | 新增【明日联机清单】(待办⑨⑪落地步骤)：kitserver 6.8.0 直链(MEGA)、两机同版本要求、中继验证判定标准、三方分工；kitserver 6.8.0 官方直链确认自 ultigamerz 页面(MEGA 网盘)

| 2026-09-05 23:17 | ⑨Kitserver 版本记录以官方 history.txt 为权威源更正：最新 6.8.2(2021-04-22)>6.8.1>6.8.0(用户持有,联机够用)；6.8.x 二进制走社区渠道分发

| 2026-09-06 09:52 | 新增 kitserver 分辨率自适应：一键启动主机【1/6】/客机【2/5】比对 kitserver\kload.cfg 的 dx.fullscreen 与 internal.resolution 四项和当前屏幕分辨率，不一致自动改写(新增 scripts\kit_resolution.ps1，幂等)；本机实测 1920x1080→2560x1440 改写+MATCH 幂等两态均验证；kitserver(9.3M, 6.8.0) 已入库并进 make_release 同步清单(含 kload.cfg 校验)

| 2026-09-06 09:52 | 新增 kitserver 分辨率自适应：一键启动主机【1/6】/客机【2/5】比对 kitserver\kload.cfg 的 dx.fullscreen 与 internal.resolution 四项和当前屏幕分辨率，不一致自动改写(新增 scripts\kit_resolution.ps1，幂等)；本机实测 1920x1080→2560x1440 改写+MATCH 幂等两态均验证；kitserver(9.3M, 6.8.0) 已入库并进 make_release 同步清单(含 kload.cfg 校验)

| 2026-09-06 10:10 | 修复 kitserver 分辨率检测两处：①DPI 缩放陷阱——PrimaryScreen.Bounds 返回逻辑分辨率(4K@150%=2560x1440)，改用 WMI Win32_VideoController 读物理分辨率(实测 3840x2160)；②多显示器——检测到多个不同分辨率时列出让用户选择(朋友 Win7 用 Get-WmiObject 兼容)；make_release.bat 已确认含 /R:1 /W:1 与 kitserver 清单并完成同步；8191 管理页 ERR_SSL_VERSION_OR_CIPHER_MISMATCH 定性：2006 年代栈的老 TLS 与现代浏览器不兼容，服务本身正常，非必需页面

| 2026-09-06 10:10 | 修复 kitserver 分辨率检测两处：①DPI 缩放陷阱——PrimaryScreen.Bounds 返回逻辑分辨率(4K@150%=2560x1440)，改用 WMI Win32_VideoController 读物理分辨率(实测 3840x2160)；②多显示器——检测到多个不同分辨率时列出让用户选择(朋友 Win7 用 Get-WmiObject 兼容)；make_release.bat 已确认含 /R:1 /W:1 与 kitserver 清单并完成同步；8191 管理页 ERR_SSL_VERSION_OR_CIPHER_MISMATCH 定性：2006 年代栈的老 TLS 与现代浏览器不兼容，服务本身正常，非必需页面

| 2026-09-06 10:40 | ⑧汉化落地：用户已将中文 e_text.afs 替换进 dev/release 两侧(双侧 MD5 一致, roster hash 同步变化不冲突; afs2fs 无覆盖冲突)；明日清单新增=新 e_text.afs 必须同步给朋友，否则开局 roster 比对失败

【维护约定】本文档随每次改动同步更新，不另行通知。
